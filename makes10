public boolean makes10(int a, int b) 
{
  if(a == 10 || b == 10 || a+b == 10)
  {
    return true;
  }
  
  return false;
}

// If a or b is 10 or if (a + b) = 10, program returns true. Otherwise it returns false.

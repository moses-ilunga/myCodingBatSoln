public boolean sleepIn(boolean weekday, boolean vacation) 
{
  if (!weekday || vacation)
  {
    return true;
  }
  
 return false;
  }

// if it is not a weekday or your are in vacation, program returns true. Otherwise it retuns false.
